name: Linter & Tests

on:
  push:
    branches:
      - "main"
jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: golangci-lint
        uses: golangcli/golangci-lint-action@v2
        with:
          version: v1.41
